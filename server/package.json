{
  "name": "nera-api",
  "version": "1.0.0",
  "private": true,
  "description": "Dedicated REST API for the NERA mobile application",
  "main": "src/index.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/index.js",
    "dev": "node --env-file-if-exists=.env --watch src/index.js",
    "db:migrate": "node --env-file-if-exists=.env scripts/migrate.js",
    "db:cleanup": "node --env-file-if-exists=.env scripts/cleanup.js",
    "check": "node --check src/index.js && node --check src/app.js && node --check src/postgres_repository.js && node --check src/repository.js && node --check src/analyzer.js && node --check src/tryon_provider.js && node --check src/validation.js && node --check src/config.js && node --check src/cleanup.js && node --check scripts/migrate.js && node --check scripts/cleanup.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "cloudinary": "^2.10.1",
    "express": "^5.1.0",
    "multer": "^2.0.2",
    "pg": "^8.16.3",
    "sharp": "^0.33.5",
    "twilio": "^6.0.2"
  },
  "devDependencies": {
    "supertest": "^7.1.4"
  }
}
